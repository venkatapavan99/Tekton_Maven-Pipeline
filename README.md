# Tekton_Maven-Pipeline
1.Created the task for cloning the git repository
2.Created the task to build the maven package using POM.xml 
3.Created the pipeline with git clone and build yaml files and JAR file was created
